[phases.setup]
nixPkgs = ["nodePackages.pnpm", "nodejs"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["npx prisma generate", "pnpm run build"]

[start]
cmd = "pnpm run start:prod"
